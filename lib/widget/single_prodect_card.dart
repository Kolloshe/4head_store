import 'package:flutter/material.dart';

class SingleProdectCard extends StatelessWidget {
  const SingleProdectCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}